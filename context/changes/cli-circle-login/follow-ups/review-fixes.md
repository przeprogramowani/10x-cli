# Follow-ups from the implementation review (deferred on 2026-09-13)

Deferred under the operator-delegated triage of `reviews/impl-review.md`. None blocks the launch, the Toolkit deploy with `AUTH_CIRCLE_LOGIN = "disabled"` or the pilot. Each item names the pilot evidence that should exist before it is picked up, so the fix is sized by data rather than by guesswork.

| Id | Item | Evidence expected from the pilot | Sketch of the fix |
| --- | --- | --- | --- |
| F3 | `start` reserves admission → account → community; a community denial does not release the earlier account reservation, so the caller also loses the 60 s cooldown and one hour/day slot. | `auth.circle.budget.denied` lines with `kind: community` during the pilot (any). If the community bucket never trips at pilot volume, leave as is. | Reserve community before account, or add `AuthBudget.release(kind)` called on a later denial. One route test per order. |
| F4 | A Circle-side `429` on the messaging endpoint is classified `rejected`, so the login is denied and the CLI reports `dm_rejected` ("use email"). Safe, but conflates throttling with a permanent refusal. | `auth.circle.transport` lines with `result: rejected`, `status: 429`. Count and spacing decide whether a distinct path is worth it. | Map upstream 429 to a route `429 rate_limited` with `retry_after_s` (or `unknown` with retry guidance); update `transport.test.ts`, the how-to and the CLI copy. This is a contract change and was explicitly kept out of the pre-launch scope. |
| F5 | `alarm()` deletes rows but keeps the SQLite tables, so each finished login leaves a small persisted object; `triggerAlarm()` is a test-only RPC on the production class (reachable only through the e2e fixture entry). | Durable Object count and stored bytes for `DeviceLogin` after the first pilot week (dashboard read). | `ctx.storage.deleteAll()` in `alarm()` with lazy DDL on the next call; move `triggerAlarm` behind the fixture module. Needs the workerd DO lane re-run. |
| F9c | `@clack/prompts` registers its own SIGINT listener, so Ctrl-C during the email poll now unwinds through `pollVerifySession`, which has no abortable sleep (correct result, longer wait). | Any user report of a slow Ctrl-C on email login. | Pass the same `AbortSignal` into `pollVerifySession`. Touches the email path, which the plan keeps byte-for-byte as 1.20.0 for this change. |
